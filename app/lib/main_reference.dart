import 'package:flutter/material.dart';

import 'reference_app.dart';

/// Fixture-only visual review target. Never use this entrypoint for a release artifact.
void main() => runApp(const ReferenceShowUpApp());
